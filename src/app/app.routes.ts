import { Routes } from '@angular/router';
import { AppChartlyComponent } from './app-chartly/app-chartly.component';

export const routes: Routes = [
    { path: '', component: AppChartlyComponent }
];
